{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://workflow-kit/schemas/artifacts/FD.schema.json",
  "title": "FD frontmatter (Feature Definition)",
  "description": "Required YAML frontmatter for a FD (Feature Definition) document. category MUST be \"Feature Definition\". Extra frontmatter keys are allowed.",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "date created",
    "tags",
    "category"
  ],
  "properties": {
    "date created": {
      "type": "string",
      "format": "date",
      "description": "Creation date (YYYY-MM-DD)."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "description": "Relevant tags."
    },
    "category": {
      "const": "Feature Definition",
      "description": "Must be exactly \"Feature Definition\" for a FD document."
    }
  }
}
