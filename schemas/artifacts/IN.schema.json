{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://workflow-kit/schemas/artifacts/IN.schema.json",
  "title": "IN frontmatter (Initiative log)",
  "description": "Required YAML frontmatter for a IN (Initiative log) document. category MUST be \"Initiative Log\". Extra frontmatter keys are allowed.",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "date created",
    "tags",
    "category"
  ],
  "properties": {
    "date created": {
      "type": "string",
      "format": "date",
      "description": "Creation date (YYYY-MM-DD)."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "description": "Relevant tags."
    },
    "category": {
      "const": "Initiative Log",
      "description": "Must be exactly \"Initiative Log\" for a IN document."
    }
  }
}
