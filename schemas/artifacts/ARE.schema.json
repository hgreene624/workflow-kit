{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://workflow-kit/schemas/artifacts/ARE.schema.json",
  "title": "ARE frontmatter (Agent Report)",
  "description": "Required YAML frontmatter for a ARE (Agent Report) document. category MUST be \"Report\". Extra frontmatter keys are allowed.",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "date created",
    "tags",
    "category",
    "status"
  ],
  "properties": {
    "date created": {
      "type": "string",
      "format": "date",
      "description": "Creation date (YYYY-MM-DD)."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "description": "Relevant tags."
    },
    "category": {
      "const": "Report",
      "description": "Must be exactly \"Report\" for a ARE document."
    },
    "status": {
      "type": "string",
      "description": "Lifecycle status of the document."
    }
  }
}
