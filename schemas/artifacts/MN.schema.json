{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://workflow-kit/schemas/artifacts/MN.schema.json",
  "title": "MN frontmatter (Meeting Note)",
  "description": "Required YAML frontmatter for a MN (Meeting Note) document. category MUST be \"Meeting\". Extra frontmatter keys are allowed.",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "date created",
    "tags",
    "category"
  ],
  "properties": {
    "date created": {
      "type": "string",
      "format": "date",
      "description": "Creation date (YYYY-MM-DD)."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "description": "Relevant tags."
    },
    "category": {
      "const": "Meeting",
      "description": "Must be exactly \"Meeting\" for a MN document."
    }
  }
}
