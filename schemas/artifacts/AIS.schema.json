{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://workflow-kit/schemas/artifacts/AIS.schema.json",
  "title": "AIS frontmatter (AI Meeting Summary)",
  "description": "Required YAML frontmatter for a AIS (AI Meeting Summary) document. category MUST be \"Meeting\". Extra frontmatter keys are allowed.",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "date created",
    "tags",
    "category"
  ],
  "properties": {
    "date created": {
      "type": "string",
      "format": "date",
      "description": "Creation date (YYYY-MM-DD)."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "description": "Relevant tags."
    },
    "category": {
      "const": "Meeting",
      "description": "Must be exactly \"Meeting\" for a AIS document."
    }
  }
}
