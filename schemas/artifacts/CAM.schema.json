{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://workflow-kit/schemas/artifacts/CAM.schema.json",
  "title": "CAM frontmatter (Campaign)",
  "description": "Required YAML frontmatter for a CAM (Campaign) document. category MUST be \"Campaign\". Extra frontmatter keys are allowed.",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "date created",
    "tags",
    "category"
  ],
  "properties": {
    "date created": {
      "type": "string",
      "format": "date",
      "description": "Creation date (YYYY-MM-DD)."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "description": "Relevant tags."
    },
    "category": {
      "const": "Campaign",
      "description": "Must be exactly \"Campaign\" for a CAM document."
    }
  }
}
