{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://workflow-kit/schemas/artifacts/KBP.schema.json",
  "title": "KBP frontmatter (KB Prompt)",
  "description": "Required YAML frontmatter for a KBP (KB Prompt) document. category MUST be \"KB Prompt\". Extra frontmatter keys are allowed.",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "date created",
    "tags",
    "category"
  ],
  "properties": {
    "date created": {
      "type": "string",
      "format": "date",
      "description": "Creation date (YYYY-MM-DD)."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "description": "Relevant tags."
    },
    "category": {
      "const": "KB Prompt",
      "description": "Must be exactly \"KB Prompt\" for a KBP document."
    }
  }
}
