{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://workflow-kit/schemas/artifacts/SO.schema.json",
  "title": "SO frontmatter (Structure Outline)",
  "description": "Required YAML frontmatter for a SO (Structure Outline) document. category MUST be \"Plan\". Extra frontmatter keys are allowed.",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "date created",
    "tags",
    "category",
    "status",
    "source"
  ],
  "properties": {
    "date created": {
      "type": "string",
      "format": "date",
      "description": "Creation date (YYYY-MM-DD)."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "description": "Relevant tags."
    },
    "category": {
      "const": "Plan",
      "description": "Must be exactly \"Plan\" for a SO document."
    },
    "status": {
      "type": "string",
      "description": "Lifecycle status of the document."
    },
    "source": {
      "type": "string",
      "description": "Wikilink to the source spec/request this derives from."
    }
  }
}
