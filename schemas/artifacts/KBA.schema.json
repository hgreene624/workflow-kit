{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://workflow-kit/schemas/artifacts/KBA.schema.json",
  "title": "KBA frontmatter (KB Analysis)",
  "description": "Required YAML frontmatter for a KBA (KB Analysis) document. category MUST be \"Report\". Extra frontmatter keys are allowed.",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "date created",
    "tags",
    "category"
  ],
  "properties": {
    "date created": {
      "type": "string",
      "format": "date",
      "description": "Creation date (YYYY-MM-DD)."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "description": "Relevant tags."
    },
    "category": {
      "const": "Report",
      "description": "Must be exactly \"Report\" for a KBA document."
    }
  }
}
